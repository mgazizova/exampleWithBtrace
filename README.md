This project is example of running code with btrace-agent for tracing methods. 

#### **How to run project**
1. Open the _runner.sh_ file and type correct PATH_TO_BTRACE.
2. Run script 
`sh runner.sh`
3. Find report in _out_ directory
